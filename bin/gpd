#!/bin/bash

set -e

me_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

me=$(basename "$0")
USAGE="Usage: $me vm [<options>]
   or: $me laptop [<options>]"

forward=gpd-$1
if [[ -x $forward ]]; then
    "$me_dir/$forward" "${@:2}"
else
    printf "$USAGE\n"
    exit 1
fi

